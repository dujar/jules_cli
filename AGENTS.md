# Agent Instructions for jules_cli

This document provides instructions for AI agents working on the `jules_cli` repository.

## Project Overview

`jules_cli` is a command-line interface (CLI) tool for [describe the purpose of the CLI tool]. It is written in [programming language, e.g., Python].

## Getting Started

To get started with development, you'll need to set up your environment.

1.  **Clone the repository:**
    ```bash
    git clone <repository_url>
    cd jules_cli
    ```

2.  **Install dependencies:**
    This project uses [dependency manager, e.g., pip] for managing dependencies. To install them, run:
    ```bash
    pip install -r requirements.txt
    ```
    *(Note: If `requirements.txt` does not exist, you may need to create it based on the project's imports.)*

## How to Run the Application

You can run the CLI tool using the following command:

```bash
python -m jules_cli.main [command] [options]
```

Replace `[command]` and `[options]` with the actual commands and options for the CLI.

## How to Run Tests

This project uses [testing framework, e.g., pytest] for testing. To run the tests, use the following command:

```bash
pytest
```

Make sure you have installed the development dependencies, which might include `pytest`.

## Coding Conventions

- Follow the [PEP 8 style guide](https.p//peps.python.org/pep-0008/) for Python code.
- Write clear and concise commit messages.
- Add unit tests for new features and bug fixes.

## Submitting Changes

Before submitting your changes, please ensure that:
1.  All tests pass.
2.  Your code is formatted according to the style guide.
3.  You have updated the documentation if necessary.

Use the `request_code_review()` tool before submitting your work.
